/**
 * API service layer — placeholder for future backend integration.
 * Currently this is a static website and no API calls are made.
 * All data comes from src/data/ and src/config/ files.
 *
 * If a backend is added later, implement API calls here using
 * axios or fetch, and import from this file in components.
 */

// import axios from 'axios';
// const apiClient = axios.create({ baseURL: import.meta.env.VITE_API_URL });

// export const fetchProducts = () => apiClient.get('/products');
// export const submitContactForm = (data) => apiClient.post('/contact', data);
